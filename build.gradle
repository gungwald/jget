apply plugin: 'java'
apply plugin: 'eclipse'

sourceCompatibility = 1.5
version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'JGet',
                   'Implementation-Version': version,
		   'Main-Class': 'com.accumulatorx.jget.JGet'
    }
}

repositories {
    mavenCentral()
}
