#!/bin/sh

if [ -n "$JAVA_HOME" ]
then
	JAVAC="$JAVA_HOME"/bin/javac
else
	JAVAC=javac
fi

if [ ! -d classes ]
then
    mkdir classes
fi

echo Compiling with $JAVAC
"$JAVAC" -sourcepath src -d classes -g -source 1.4 -target 1.4 src/*.java
