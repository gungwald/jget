#!/bin/sh

echo This will only generate a Java 1.1 class file if javac 1.1 is used.

if [ -n "$JAVA_HOME" ]
then
	JAVAC="$JAVA_HOME"/bin/javac
else
	JAVAC=javac
fi
cd src
echo Compiling with $JAVAC
"$JAVAC" -d ../classes -g JGet.java